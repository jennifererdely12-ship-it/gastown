[build]
  publish = "public"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Clean /api/* paths for the widget and dashboard.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
